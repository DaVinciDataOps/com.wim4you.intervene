<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <attribution android:label="@string/tag_patrol_service" android:tag="patrol_service" />
    <attribution android:label="@string/tag_distress_service" android:tag="distress_service" />
    <attribution android:label="@string/tag_distress_alarm" android:tag="distress_alarm" />
    <attribution android:label="@string/tag_tracker_service" android:tag="tracker_service" />
    <attribution android:label="@string/tag_distress_notification_service" android:tag="distress_messaging_service" />

    <application
        android:name=".InterVeneApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Intervene" >
        <service
            android:name=".distress.DistressMessagingService"
            android:exported="false"
            android:attributionTags="distress_messaging_service">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".location.LocationTrackerService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:attributionTags="tracker_service" />
        <receiver android:name=".location.LocationTrackerReceiver" />
        <service
            android:name=".distress.DistressService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:attributionTags="distress_service" />
        <service
            android:name=".location.PatrolService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:attributionTags="patrol_service" />
        <receiver android:name=".location.PatrolReceiver" />
        <service
            android:name=".distress.DistressSoundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:attributionTags="distress_alarm" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${google_maps_api_key}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Intervene.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>